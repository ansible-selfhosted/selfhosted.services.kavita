# skip jinja testing
[Unit]
Description=httpd container service

[Container]
ContainerName=my-apache-app
Image=docker.io/library/httpd:2.4
PublishPort={{ published_port }}:80

[Service]
Restart=always

[Install]
WantedBy=multi-user.target default.target